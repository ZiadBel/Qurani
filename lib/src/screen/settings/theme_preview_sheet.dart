import "package:flutter/material.dart";

/// Theme preview sheet — deprecated.
/// Qurani now uses fixed Light/Dark palettes (Ayah-inspired).
/// Kept as stub for any remaining references.
class ThemePreviewSheet extends StatelessWidget {
  const ThemePreviewSheet({super.key});

  static Future<void> show(BuildContext context) async {}

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

