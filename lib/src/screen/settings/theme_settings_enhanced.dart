import "package:flutter/material.dart";

/// Enhanced theme color picker — deprecated.
/// Qurani now uses fixed Light/Dark palettes (Ayah-inspired).
/// Kept as stub for any remaining references.
class ThemeSettingsEnhanced extends StatelessWidget {
  const ThemeSettingsEnhanced({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
